{
    "SCENE-DEFAULTS": {
        "settings": {
            "config": {
                "title": "Default Scene Settings",
                "label": "Configure",
                "hint": "Configure the default scene settings for new scenes being made"
            }
        }
    }
}