
    "SCENE-DEFAULTS": {
        "settings": {
            "config": {
                "title": "默认场景设置",
                "label": "配置",
                "hint": "为新创建的场景配置默认场景设置"
            }
        }
    }
}