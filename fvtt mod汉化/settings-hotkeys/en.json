{
  "SettingsHotkeys": {
    "Setting": {
      "CloseBehavior": {
        "Name": "Behavior of Repeated Hotkey Presses",
        "Hint": "Close (default): Close the window unconditionally \n Focus/Close: Focus the window if not currently focused, otherwise close \n Focus: Only focus the window if not currently focused, never close",
        "Choices": {
          "close": "Close",
          "focusClose": "Focus/Close",
          "focus": "Focus"
        }
      }
    },
    "ToggleSettings": "Toggle Configure Settings",
    "ToggleControls": "Toggle Configure Controls",
    "ToggleModules": "Toggle Module Management"
  }
}
