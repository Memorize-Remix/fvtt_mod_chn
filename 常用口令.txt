提交推送
# 添加修改的文件到暂存区
git add "fvtt mod汉化/magicitems/cn.json"

# 如果你还修改了其他文件，可以全部添加
# git add "fvtt mod汉化/magicitems/cn.json" "fvtt mod汉化/inspect-statblock/cn.json"

# 提交更改（记得写清晰的提交信息）
git commit -m "修复 magicitems/cn.json 的 JSON 语法错误"

# 推送到远程仓库
git push


放弃当前合并，重新拉取
# 1. 放弃当前的合并状态
git merge --abort

# 2. 重新拉取远程更新
git pull origin master

# 3. 如果有冲突，解决后提交；如果没有冲突，直接推送
git push origin master